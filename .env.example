# A Pioneer key automatically enables real inference.
# Without it, the deterministic mock is used.
PIONEER_API_KEY=

# Real Band routing needs three registered remote agents in one chat room.
BAND_ROOM_ID=
BAND_AGENT_A_API_KEY=
BAND_AGENT_B_API_KEY=
BAND_AGENT_C_API_KEY=
